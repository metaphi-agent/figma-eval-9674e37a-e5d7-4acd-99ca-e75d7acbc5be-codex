# Landing Page Kit (Figma Eval)

React + Vite + Tailwind v4 app that previews exported Figma frames (desktop + mobile).

## Dev

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```
